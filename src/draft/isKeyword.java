package draft;
import java.util.ArrayList;
public class isKeyword {

	public static void main(String[] args) {
		ArrayList<String> supplierNames = new ArrayList<String>(); 
		//List<String> immutableList = List.of("one", "two", "three");	
		System.out.println(supplierNames.get(1));
		
		/*
		List<String> keywords = new ArrayList<>(
				Arrays.asList(1, 2, 3)); 

		
		
		System.out.println("List : " + keywords.toString()); 
		Scanner input = new Scanner(System.in);

		String key = input.next();
		boolean isExist = false;
		for (String item : classesList) { 
		    if (key.equals(classesList.get(counter))) {
		        //Your logic if it's there.
		         isExist = true;
		         break;
		   }
		}
		key.close();
		 if(isExist) {
		     //Found in the arraylist.
		 }
		 
		 */
	}
}
